import React from "react";
import { BrowserRouter, Routes } from "react-router-dom";

const App = () => {
	return (
		<div>
			<BrowserRouter>
				<Routes>{/* Pages */}</Routes>
			</BrowserRouter>
		</div>
	);
};

export default App;
